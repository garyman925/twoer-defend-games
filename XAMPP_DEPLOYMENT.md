# 🚀 在 XAMPP 上部署遊戲指南

## 📋 步驟說明

### 1️⃣ 安裝依賴（首次執行）

在專案目錄中打開命令提示字元（CMD）或 PowerShell，執行：

```bash
npm install
```

這會安裝所有必要的依賴，包括新增的 `copy-webpack-plugin`。

---

### 2️⃣ 打包遊戲

執行以下命令來打包遊戲：

```bash
npm run build
```

這會：
- ✅ 將所有 JavaScript 打包成 `bundle.js`
- ✅ 複製所有 `assets` 資源到 `dist/assets`
- ✅ 複製獨立的 HTML 頁面（my-ship.html、shop.html 等）
- ✅ 生成最終的 `index.html`

**打包完成後，你會看到：**
```
✔ webpack 5.89.0 compiled successfully
```

---

### 3️⃣ 在 XAMPP 上訪問

打包完成後，`dist` 文件夾包含了所有可以直接運行的文件。

#### 方法 A：直接在瀏覽器打開（推薦）

打開瀏覽器，訪問：

```
http://localhost/games/tower-defend_v2/dist/
```

或者直接在 Windows 檔案總管中雙擊：
```
C:\xampp\htdocs\games\tower-defend_v2\dist\index.html
```

#### 方法 B：設定虛擬主機（可選）

在 XAMPP 的 `httpd.conf` 中添加：
```apache
<Directory "C:/xampp/htdocs/games/tower-defend_v2/dist">
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
```

---

### 4️⃣ 文件結構

打包後的 `dist` 目錄結構：

```
dist/
├── index.html              # 主遊戲頁面（帶 bundle.js）
├── bundle.js              # 打包後的遊戲代碼
├── my-ship.html           # 我的戰機頁面
├── shop.html              # 商店頁面
├── leaderboard.html       # 排行榜頁面
├── quiz.html              # 知識問答頁面
└── assets/                # 所有遊戲資源
    ├── audio/
    ├── css/
    ├── data/
    ├── maps/
    ├── sprites/
    └── ui/
```

---

## 🔄 開發流程

### 開發時（使用 webpack-dev-server）

```bash
npm run dev
```
- 訪問：`http://localhost:3000`
- 自動熱重載
- 方便調試

### 部署時（打包後在 XAMPP）

```bash
npm run build
```
- 訪問：`http://localhost/games/tower-defend_v2/dist/`
- 正式環境
- 性能優化

---

## ⚙️ 配置說明

### 關鍵配置項（webpack.config.js）

```javascript
output: {
  publicPath: './',  // 🔑 使用相對路徑，確保 XAMPP 可訪問
}
```

### 資源複製（CopyWebpackPlugin）

所有靜態資源會自動複製到 `dist` 目錄：
- ✅ assets/ 完整複製
- ✅ 獨立 HTML 頁面複製
- ✅ 保持原有目錄結構

---

## 🐛 常見問題

### ❓ 打包後資源找不到？

**解決方案**：檢查資源路徑是否正確
```javascript
// ✅ 正確：相對路徑
'assets/sprites/player.png'

// ❌ 錯誤：絕對路徑
'/assets/sprites/player.png'
```

### ❓ CSS 樣式沒有載入？

**原因**：CSS 已經被打包到 bundle.js 中了
- 檢查瀏覽器控制台是否有錯誤
- 確認 bundle.js 正確載入

### ❓ 獨立 HTML 頁面（my-ship.html）無法訪問遊戲？

**解決方案**：獨立頁面需要返回主頁面
```javascript
// 在 my-ship.html 等頁面中
window.location.href = 'index.html';  // 返回主遊戲
```

### ❓ 打包後文件太大？

**解決方案**：使用生產模式打包
```bash
npm run build  # 已經是 production 模式
```

這會：
- 壓縮代碼
- 移除 source map
- 優化資源

---

## 📊 打包大小優化建議

### 當前配置
- **開發模式**：包含 source map，方便調試
- **生產模式**：代碼壓縮，性能優化

### 進一步優化（可選）

1. **圖片壓縮**：使用 `image-webpack-loader`
2. **代碼分割**：使用 `splitChunks`
3. **懶加載**：動態導入場景

---

## 🎯 快速指令參考

```bash
# 安裝依賴
npm install

# 開發模式（webpack-dev-server）
npm run dev

# 打包生產版本
npm run build

# 清理並重新打包
rm -rf dist && npm run build
```

---

## ✅ 驗證清單

部署前檢查：

- [ ] 執行 `npm install` 安裝依賴
- [ ] 執行 `npm run build` 打包成功
- [ ] `dist` 目錄已生成
- [ ] 在瀏覽器訪問 `http://localhost/games/tower-defend_v2/dist/`
- [ ] 遊戲可以正常啟動
- [ ] 所有資源正常載入
- [ ] 獨立頁面（my-ship.html 等）可以訪問

---

## 📝 更新說明

**修改內容**：
1. ✅ webpack.config.js - 添加 `publicPath: './'`
2. ✅ webpack.config.js - 添加 CopyWebpackPlugin
3. ✅ package.json - 添加 copy-webpack-plugin 依賴
4. ✅ 資源路徑配置為相對路徑

**日期**：2025-11-07
**版本**：1.0.0

---

🎮 現在您可以開始打包並在 XAMPP 上運行遊戲了！

